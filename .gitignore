__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.pytest_cache/
logs/*.log
logs/*.db
# local session transcript — contains personal info, never push
context1.docs
dashboard/node_modules/
dashboard/dist/
.vite/
.DS_Store
*.tlog
*.bin
sitl/binaries/
.env
# VanniKawachh hub runtime artifacts (deployment-local, never push)
hub/clips/
hub/nodes.json
hub/cert.pem
hub/key.pem
logs/demo_phase0/
logs/recordings/
# ML dataset + trained artifacts (large / generated; keep code, not data)
ml/data/
ml/out/
ml/testset/
# ...but keep the small trained model so the hub + phone-test work out of the box
!ml/out/
ml/out/*
!ml/out/stage1_nn.npz
# ...and the GPU-trained int8 Stage-1 model + its honest metrics + firmware array
!ml/out/stage1_int8.tflite
!ml/out/stage1_metrics.json
!ml/out/stage1_model_data.cc
# local YAMNet eval clips (regenerate: ml/testclips/make_clips.py)
ml/testclips/*.wav
dronecontextgpu.txt
