# Environment for the hub service on the permanent GPU machine.
# The empty control URL disables Lightning-style browser start/stop controls:
# systemd, not a web request, owns the always-on simulation lifecycle.
GAZEBO_CONTROL_URL=
GAZEBO_VIEW_URL=/gazebo-ui/vnc.html?autoconnect=true&path=gazebo-ui/websockify
