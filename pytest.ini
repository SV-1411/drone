[pytest]
testpaths = tests
# Collect all unit test files. The e2e SITL flight (test_full_mission.py) is a
# standalone script that spawns SITL, not a pytest module, so ignore it here;
# run it directly with: python tests/test_full_mission.py
addopts = --ignore=tests/test_full_mission.py
